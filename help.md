# getting help

* man ls：lsコマンドのオプション等のマニュアルを確認できる
* -h, --helpをつけてhelp参照(ls -h) あるのとないのがある

## man操作
* space：ページ送り
* q：quit マニュアル
* shift g(G)：最後に飛ぶ

* ls /bin => manで探す勉強法

## which
* コマンドのフルパスがわかる（例：which ls)
